services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env.template
    volumes:
      - .:/app
      - /app/static
      - /app/templates
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    restart: unless-stopped
